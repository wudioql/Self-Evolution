# ═══════════════════════════════════════════════════════════
# Self-Evolution · .gitignore
# 原则：仓库只放「可公开、可复用、有长期价值」的东西
# ═══════════════════════════════════════════════════════════

# ── 1. music-log：受版权保护的音频与工程文件 ──────────────
# 原曲音频 / 分轨干轨 = 他人作品的复制，绝不可入库（DMCA 风险）
music-log/**/original/
music-log/**/stems/
*.mp3
*.wav
*.flac
*.aiff
*.ogg
*.m4a

# MIDI 是扒带的主产出，但法律上属原曲旋律的转录（派生作品）。
# 默认不入库；例外须先确认版权与本人授权，不用强制添加绕过这道边界。
*.mid
*.midi

# 歌词 = 原作品的文字复制，与音频同等对待
*.lrc

# DAW 工程（二进制，且常含插件/路径等本机信息）
*.song
*.songx
*.als
*.flp
*.cpr
*.logicx

# ── 2. 未脱敏的本地专属内容 ──────────────────────────────
# 真实机台编号 / 报警代码 / 薪资等，所有格式用 *.local.* 后缀，不入库
# （*.local.md / 01-个人档案.local.md 等都是 *.local.* 的子集，不重复列）
*.local.*

# ── 3. 工具导出的本地数据 ────────────────────────────────
# JSON 是正本，HTML / Markdown 为生成视图（见 AGENTS.md §7）；
# 原始故障与自动回滚副本绝不提交公开 Git（progress/*.local.json 已被 §2 的 *.local.* 覆盖）
tools/*.backup.json

# ── 4. agent 本地设置 ───────────────────────────────────
# 仅保留 .agents/skills/ 的 vendored 副本；当前未安装 .claude/skills/ 或符号链接
.claude/settings.local.json

# ── 5. 系统垃圾 ──────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────
.idea/
.vscode/
*.iml
.trae/
.workbuddy/

# 历史完整内容打包
*.zip.txt

# 本地自动备份 / 协作锁 / 临时文件
.backups/
*.lock
*.tmp
__pycache__/
*.pyc
