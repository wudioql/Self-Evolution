# AGENTS.md

> **面向 AI agent 的接管说明。人类用户请先读 [`README.md`](README.md)。**
> 本文件只写"操作规范"，不解释"为什么"（原因在 [`02-方向分析.md`](02-方向分析.md)）。
> 最后更新：2026-09-06

---

## 1. 这是什么

**wudioql**（GitHub: **https://github.com/wudioql**）的业余学习体系。
已确认主线 = **声音（90 天发布 3 首歌）**，职业走**设备纵深**，**Day 0 = 2026-09-06（周日）**，Day 1 = 09/07；Day 90 = 12/05，14 周于 Day 98 = 12/13 收尾。

---

## 2. 目录：六层结构

| 层 | 位置 | 定位 | 写法要求 |
|---|---|---|---|
| **交接** | `AGENTS.md` / `README.md` | 入口 | 极简。agent 规范在本文件，人类说明在 README |
| **速查** | `00-速查表.md` | 随身卡片 / 贴墙 | **只放硬事实与数字，不写任何解释**。每节标 `→` 指向详解 / 唯一来源（导航节除外） |
| **基础** | `01-个人档案.md`<br>`02-方向分析.md` | 输入 / 决策 | 01 = 本人事实与已确认决策，推论须标注<br>02 = 历史论证，不作现行待办 |
| **执行** | `03-90天计划.md`<br>`04-扒带作业单.md` | 要做的动作 | **必须**绑定日期 / 具体作品 / 可勾选 / 有产出物。**不解释为什么** |
| **参考** | `manuals/*.md` | 查，不通读 | 字典式。按主题跳读。每份结尾必须有 **「与执行层的对接」** 章节 |
| **工具** | `tools/*.html` | 单文件离线应用 | 见 §7 |

> **六层说的是"文档体系"。** 另有配套目录不属于任何一层（见 README §3）：
> `music-log/` = 产出 · `progress/` = 计划与状态 · `data/` = 本地故障 · `scripts/` + `.github/` = 维护 · `.agents/skills/` = 第三方 skill；`AUDIT.md` = 已核对范围与待修缺口、`CHANGELOG.md` = 变更记录，都不是学习任务。

> 📌 **三条边界说明**（改执行层前先看这里）：
>
> **①「可勾选」怎么落地 —— `03` 和 `04` 不一样：**
> - `04` 的 `- [ ]` 是作业步骤模板，不当进度账；小步骤完成先写对应任务备注。
> - **排期与状态唯一来源 = `progress/plan90.json`**；稳定任务 ID 不按位置重编。
> - `progress/进度总览.md` / HTML / `03` 的逐周表是自动生成视图，不能手改；运行 `scripts/sync-plan.py`。
> - `03` 不再维护第二套复选框；生成的总览显示勾选不构成第二个真相源。
>
> **②「不解释为什么」的边界：**
> - 执行层允许**每节几行**的"为什么"（`03` §1 每条轨道一句话、`04` 每首歌为什么选它）——
>   它提升执行意愿，且体量受限。
> - ⛔ 但**论证的完整版一律在 [`02-方向分析.md`](02-方向分析.md)**。
>   发现执行层在论证 → 把论证搬去 02，执行层留结论 + 一行 `→ 02 §X`。

> **③ 速查层「每节必须有 `→`」的例外：**
> `00` §11「文件在哪」本身就是导航索引，每一行都是文件链接 —— **不要求它再指向手册**。其余各节必须有。
>

---

## 3. 接管必读顺序

1. **本文件**
2. **[`01-个人档案.md`](01-个人档案.md)** —— 尤其 **§7 失败模式**、**§8 已确认的决策**、**§9 硬性约束**
3. **[`progress/plan90.json`](progress/plan90.json)** + **[`03-90天计划.md`](03-90天计划.md)** —— 实际状态 / 规则；有本地故障文件时按需读 `data/faults.local.json`
4. 按需读 `manuals/` 对应主题；操作命令与数据边界见 AI 手册 §4.6；维护 / 导入前读 [`AUDIT.md`](AUDIT.md) 的修复状态与长期边界

---

## 4. 三条不要

| ⛔ | 不要 | 原因 |
|---|---|---|
| 1 | **不要新增轨道** | 上限 4 条（声音 / 设备 / 日语 / 音韵）。`pub` / `key` 是发布 / 复检分类，不是新学习方向。**新增学习轨道须本人确认并先收缩原有范围** |
| 2 | **不要建议需要新硬件 / 新场地 / 大音量的方案** | 厂内单人宿舍、无钢琴。古筝 / 二胡 / 提琴 / 竹笛 / 大声练唱 全部不可行 |
| 3 | **不要给"系统学教材"类建议** | 已用三年证明无效（和弦裸背三年没记住）。**一切知识必须挂靠在具体作品或具体问题上** |

---

## 5. 每条新建议都要过的自测

> **它会不会变成第 6 根钉子？**

1. **反馈周期 ≤ 2 周吗？**（> 2 周 = 会在平台期死掉）
2. 结束时能产出一个**可见的文件**吗？（不能 = 没有反馈）
3. 它是**挂靠**在已有动作上，还是要求**新增时间块**？（新增 = 决策疲劳）

三条全过才提。

---

## 6. 修改规范

- 改**前**先说明改动范围；改**后**在 [`CHANGELOG.md`](CHANGELOG.md) 按日期倒序补一条（格式见该文件头），事件用绝对日期。日常问句、`today` 命令与回顾模板不当成历史事实。
- 复用当前目录；未经本人授权不初始化 Git、不 commit / push、不公开发布；不额外制作 ZIP。
- 沿用 UTF-8、标准库 Python 与内联 JS；无项目级 formatter / lint 配置，不为风格引入依赖。稳定 ID、schema 和生成标记不得随意改名。
- 跨层改动分步做：先唯一来源，再生成视图，最后入口说明；不在多个文件手动抄状态。
- 新增/重写 `manuals/` 里任何一份时，**必须**保持：
  - 文件头的"定位说明"（这份是参考层，不通读 + 指向执行层）
  - 结尾的「与执行层的对接」章节（表格：时间 / 执行层动作 / 本手册章节）
- **更改 [`01-个人档案.md`](01-个人档案.md) §8 的已确认决策 → 先说明原因并取得本人确认，再同步档案；手册愿景不得反向增加任务。**
- 不要把内容同时写进两处（README 与 AGENTS 已做过职责划分，避免漂移）。
- **改手册里的硬事实（数值 / 调值 / 和弦进行 / 规则条目）时，必须同步 [`00-速查表.md`](00-速查表.md)。** 两者冲突时以手册为准，并回头修正速查表。
- **[`00-速查表.md`](00-速查表.md) §11「文件在哪」必须与 [`README.md`](README.md) §3 的导航表保持一致**（改任一侧都要同步另一侧）。
### 6.1 单一真相源 ⭐ 最重要的一条

> **一句话：`manuals/` 是"怎么做"的唯一存放地。执行层只放「什么时候 · 做多少 · 做成什么样算完」。**

| 内容类型 | 放哪 | 例子 |
|---|---|---|
| **学习方法 / 原理 / 技术参数 / 学习排错** | `manuals/` 里对应的手册，**只此一处** | 混音 4 件事 · 扒带 6 步 · Praat 操作 · 故障库字段 · ACE-Step 的 8GB 应对 · 知识地图 |
| **排期 / 任务 / 验收 / 完成状态** | `progress/plan90.json`；`03` 逐周表自动生成，`04` 仅作分步模板 | "W2 扒根音" · "和声笔记及对账结果" · "好听元素清单 30 条" |
| **学习方法 / 技术数据的极简镜像** | `00-速查表.md`（**唯一被允许的例外**） | 每个 Step 一行、不写解释、必带 `→` 指向手册 |

**执行层可写短动作与验收，不再展开第二套操作教程 / 知识表。** 发现重复时严格按序处理：

1. **先确认手册里有完整版本** —— 没有就**先迁进去**。**绝不在内容还没有新家之前删掉它。**
2. 执行层只留一行 `→ 手册 §X`。
3. 若这段内容带验收标准（如"好听元素清单 30 条"），**标准留在执行层，方法去手册**。

**本人明确要求 / 01 已确认约束优先；方法看 `manuals/`，实际日期与状态看 JSON。** 改方法不覆盖完成记录。
方法 / 技术数据的人工镜像仅在 `00`；README 可保留导航和目标窗口摘要，不滚动抄状态。手册 / 04 对接表只概括初始模板，重排后以 JSON 为准。
`.agents/skills/` 是上游通用规范，不代表当前安装状态；文档常规同步不凭空创建 `docs/`，不修改上游 skill 正文或参考文件。

---

## 7. 工具与记录维护规范

日常 CLI：**Python 3.11+ 标准库**；静态 HTML 直接打开，无服务 / 数据库 / 环境变量配置。Node 20 仅维护测试用，Playwright 仅可选浏览器测试用（安装见 AI 手册 §4.6）。

| 规则 | 说明 |
|---|---|
| **零外部依赖** | `tools/*.html` 保持单文件、内联 JS / CSS / JSON，无 CDN / 外部字体 / 外部脚本 |
| **唯一来源** | 进度 `progress/plan90.json`；真实故障 `data/faults.local.json`。不是 localStorage，不再向用户索取截图查进度 |
| **浏览器边界** | 默认打开文件快照；缓存只能显式恢复为草稿，绝不自动覆盖文件。直写必须授权、串行写、检测外部改动，失败不报成功 |
| **私有视图** | 故障原始内容仅进入 `*.local.*`；公开 `tools/故障模式库.html` 必须保持空白工具壳 |
| **生成方向** | JSON → 总览 / HTML / `03` 逐周表。禁止从 HTML 反向抽取并重建 JSON |
| **导入边界** | 异常记录使整包停止，正本 / 原件不变；不猜日期、不静默过滤。未知但合法的 ID 归档，不计完成 |
| **保留状态** | 增删改排期保留 ID、done、completedOn、note、evidence、周备注、checkins、dailyLogs、history、`_orphans` 及未知字段 |
| **写入安全** | 优先用 `scripts/progress.py`；Python 路径原子写、旧内容冲突检测、`.backups/` 保留最近 20 版。不是跨进程数据库事务；浏览器直写不生成旁路备份。手改 JSON 后校验同步 |
| **校验** | 数据 / 视图一致性、HTML 标签与 JS 语法、文档及隐私检查均要通过 |

---

## 8. 每日接管协议（用户只需要对话）

1. **读文件，不凭聊天记忆**。当前日期按 **Asia/Shanghai** 计算；用户明确要查别的日期时用 `--date`。不能把旧 README 中的“今天”当今天。
2. **问任务只读**：优先给 1 件事、最多 2 件顺带动作；显示实际日期 / Day / 周次 / 最低剂量。夜班只留 10 分钟，不堆欠账；音乐落后也包括尚未通过的发布验收，不能只看制作项；IPA 不挤占音乐。
3. **完成需本人确认**：明确报告后更新对应 ID；含糊的“做完了”先核对是哪项。小步骤完成只记备注，不勾整个周目标；时间性备注用实际绝对日期。日期到了、工具建好、目录存在都不等于用户做完。
4. **打卡独立于任务**：只有用户确认实际做过并要求记录才打卡。补卡须给实际日期；不虚构连续天数。歌曲发布记录需真实链接和四项验收。
5. **写完即核验并同步**：回复实际勾了哪些 ID / 文件、还差什么；不让用户手工点页面。禁止擅自发帖、push、替用户作出缩减三首目标的决定。
6. **故障如实录入**：仅整理本人提供的信息，未知根因 / 时长 / 复发 / 解决状态留空待确认。示例不计 5 / 10 / 30 条成果；达到数量不自动勾周目标。
7. **重排有留痕**：确认受影响任务及日期后改 JSON；ID 不变、历史不删。夜班模式只暂停推荐，不自动取消任务；要顺延截止日需另行确认重排。

以下从项目根目录执行；Windows 的 Python 命令按已安装解释器替换为 `python` / `py -3`。

```bash
python3 scripts/progress.py today
python3 scripts/progress.py today --date 2026-09-07
python3 scripts/progress.py fault list
python3 scripts/sync-plan.py --check
python3 scripts/check-docs.py --verbose
python3 scripts/check-tools.py
python3 -m unittest discover -s scripts -p 'test_*.py'
node scripts/test-file-store.cjs
python3 scripts/browser-smoke.py  # 可选维护检查，使用隔离数据与固定日期
```

写入 / 撤销 / 豁免 / 导入 / 脱敏命令见 AI 手册 §4.6 与 `progress.py --help`；**没有本人确认，不照抄示例勾选或打卡**。
CI 在 `main` push、pull request 或手动触发时跑只读检查与隔离测试，不部署；测试不得污染真实记录。审计先登记代码问题；经用户授权修复后，补负例再验收，不改预期规则为 bug 开脱。

---

## 9. 禁区清单

- 新增第五条轨道
- 建议买设备 / 买乐器 / 买插件（已约定 90 天内不买）
- 把联网 / 云服务作为新执行前置：默认工作流用已有本地工具，外部网页只作可选查阅；HTML 必须离线自足。实际公开发布仍按已确认目标执行，不擅自代发
- 给"报班 / 考证 / 找老师"类建议（2027 年之前不适用）
- 把 `manuals/` 里的内容搬进执行层（会让周计划变重）
- ⛔ **提交未脱敏的内容**：真实机台编号 / 报警代码 / 薪资 / 厂区与产线规格 / 他人姓名。
  需要记的用 `*.local.*` 后缀（JSON / Markdown / HTML 均排除 Git）；进度 JSON 与普通 HTML / Markdown 中不能写真值。
- ⛔ **本地文件不等于不上云**：Arena 工作区不是用户 Windows 硬盘。企业原始日志不得带到未经授权的云端 agent；只用获准的本机环境或脱敏摘要。
- 建议用户学习编程（**已确认：他对编程能力无任何要求和预期**）

---

## 10. 一句话

> **这份体系的敌人是"更多"，不是"不够"。**
> 任何增加复杂度、增加选项、增加文件、增加轨道的改动，默认都是错的。

---

## 11. 🔒 本地真值映射（脱敏仓库的还原机制）

**公开文件使用代称；整个工作目录含私有文件，不能直接当公开发布包。** `01` / `02` / `README` / `半导体手册` 里的
"华东 A 厂""华东某市""⛔ 脱敏"都是**占位符**。

**若工作区里存在 `01-个人档案.local.md`（gitignored，CI 里没有），先读它。**
它是一张「占位符 → 真值」映射表。读档案时把真值替换回去再理解 ——
**这样你在本地看到的信息是完整的，不会因为脱敏而判断失真。**

三条硬规矩：

1. ⛔ **不要把真值写进任何产出物** —— commit message、文档、代码、issue、对话里对外的部分
2. ⛔ **不要"顺手"把真值填回公开文件** —— 那会直接污染仓库
3. ✅ 引用时继续用占位符。分析不依赖真值（"华东 A 厂"和真实厂名，对结论没区别）

> **改完任何文档后跑 `python3 scripts/check-docs.py`。**
> 它会**反着用那张映射表**，把每个真值拿到所有公开文件里搜一遍，命中即报 `隐私` 失败。
> 也就是说：**映射表同时是"真值清单"和"泄露检测词表"** —— 加一条真值，就多一道防线。
