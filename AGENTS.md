# AGENTS.md

> **面向 AI agent 的接管说明。人类用户请先读 [`README.md`](README.md)。**
> 本文件只写"操作规范"，不解释"为什么"（原因在 [`02-方向分析.md`](02-方向分析.md)）。
> 最后更新：2026-09-10

---

## 1. 这是什么

**wudioql**（GitHub: **https://github.com/wudioql**）的业余学习体系。
已确认主线 = **声音（90 天发布 3 首歌）**，职业走**设备纵深**，**Day 0 = 2026-09-06（周日）**，Day 1 = 09/07；Day 90 = 12/05，14 周于 Day 98 = 12/13 收尾。

---

## 2. 目录：六层结构

| 层 | 位置 | 定位 | 写法要求 |
|---|---|---|---|
| **交接** | `AGENTS.md` / `README.md` | 入口 | 极简。agent 规范在本文件，人类说明在 README |
| **速查** | `00-速查表.md` | 随身卡片 / 贴墙 | **只放硬事实与数字，不写任何解释**。每节标 `→` 指向详解 / 唯一来源（导航节除外） |
| **基础** | `01-个人档案.md`<br>`02-方向分析.md` | 输入 / 决策 | 01 = 本人事实与已确认决策，推论须标注<br>02 = 历史论证，不作现行待办 |
| **执行** | `03-90天计划.md`<br>`04-扒带作业单.md` | 要做的动作 | **必须**绑定日期 / 具体作品 / 可勾选 / 有产出物。**不解释为什么** |
| **参考** | `manuals/*.md` | 查，不通读 | 字典式。按主题跳读。每份结尾必须有 **「与执行层的对接」** 章节 |
| **工具** | `tools/*.html` | 单文件离线应用 | 见 §7 |

> **六层说的是"文档体系"。** 配套目录不属于任何一层（README §3）：`music-log/` 产出 · `progress/` 计划与状态 · `data/` 本地故障 · `scripts/` + `.github/` 维护 · `.agents/skills/` 第三方 skill；`AUDIT.md` = 已核对范围与待修缺口、`CHANGELOG.md` = 变更记录，都不是学习任务。

> 📌 **三条边界说明**（改执行层前先看这里）：
>
> **①「可勾选」怎么落地：** `04` 的 `- [ ]` 是作业步骤模板、不当进度账（小步骤先写对应任务备注）；排期与状态唯一来源 = `plan90.json`（§6.1），`进度总览.md` / HTML / `03` 逐周表是自动生成视图、不能手改（`scripts/sync-plan.py`），稳定任务 ID 不按位置重编，`03` 不维护第二套复选框。
>
> **②「不解释为什么」的边界：** 执行层允许**每节几行**的"为什么"（提升执行意愿）；**论证完整版一律在 [`02-方向分析.md`](02-方向分析.md)**。发现执行层在论证 → 论证搬去 02，执行层留结论 + 一行 `→ 02 §X`。
>
> **③ 速查层「每节必须有 `→`」的例外：** `00` §11「文件在哪」本身是导航索引，每一行都是文件链接，不要求再指向手册。

---

## 3. 接管必读顺序

1. **本文件**
2. **[`01-个人档案.md`](01-个人档案.md)** —— 尤其 **§7 失败模式**、**§8 已确认的决策**、**§9 硬性约束**
3. **[`progress/plan90.json`](progress/plan90.json)** + **[`03-90天计划.md`](03-90天计划.md)** —— 实际状态 / 规则；有本地故障文件时按需读 `data/faults.local.json`
4. 按需读 `manuals/` 对应主题；操作命令与数据边界见 AI 手册 §4.6；维护 / 导入前读 [`AUDIT.md`](AUDIT.md) 的修复状态与长期边界

---

## 4. 三条不要

| ⛔ | 不要 | 原因 |
|---|---|---|
| 1 | **不要新增轨道** | 上限 4 条（声音 / 设备 / 日语 / 音韵）。`pub` / `key` 是发布 / 复检分类，不是新学习方向。**新增学习轨道须本人确认并先收缩原有范围** |
| 2 | **不要建议需要新硬件 / 新场地 / 大音量的方案** | 厂内单人宿舍、无钢琴。古筝 / 二胡 / 提琴 / 竹笛 / 大声练唱 全部不可行 |
| 3 | **不要给"系统学教材"类建议** | 已用三年证明无效（和弦裸背三年没记住）。**一切知识必须挂靠在具体作品或具体问题上** |

---

## 5. 每条新建议都要过的自测

> **它会不会变成第 6 根钉子？**

1. **反馈周期 ≤ 2 周吗？**（> 2 周 = 会在平台期死掉）
2. 结束时能产出一个**可见的文件**吗？（不能 = 没有反馈）
3. 它是**挂靠**在已有动作上，还是要求**新增时间块**？（新增 = 决策疲劳）

三条全过才提。

---

## 6. 修改规范

- 改**前**先说明改动范围；改**后**在 [`CHANGELOG.md`](CHANGELOG.md) 按日期倒序补一条（格式见该文件头），事件用绝对日期。日常问句、`today` 输出与回顾模板不当成历史事实。
- 复用当前目录；未经本人授权不初始化 Git、不 commit / push、不发布、不制作 ZIP。
- 沿用 UTF-8、标准库 Python、内联 JS；不为风格引入依赖；稳定 ID / schema / 生成标记不得随意改名。
- 跨层改动分步：唯一来源 → 生成视图 → 入口说明；不在多个文件手动抄状态。
- 新增 / 重写 `manuals/` 保持文件头"定位说明" + 结尾「与执行层的对接」三列表格（check-docs 强制）。
- **更改 [`01-个人档案.md`](01-个人档案.md) §8 的已确认决策 → 先说明原因并取得本人确认，再同步档案；手册愿景不得反向增加任务。**
- 不要把内容同时写进两处（README 与 AGENTS 已做过职责划分，避免漂移）。
- **改手册里的硬事实（数值 / 调值 / 和弦进行 / 规则条目）时，必须同步 [`00-速查表.md`](00-速查表.md)**；两者冲突时以手册为准，并回头修正速查表。
- **`00` §11「文件在哪」必须与 [`README.md`](README.md) §3 的导航表保持一致**（改任一侧都要同步另一侧）。

### 6.1 单一真相源 ⭐ 最重要的一条

> **一句话：`manuals/` 是"怎么做"的唯一存放地。执行层只放「什么时候 · 做多少 · 做成什么样算完」。**

| 内容类型 | 放哪 | 例子 |
|---|---|---|
| **学习方法 / 原理 / 技术参数 / 学习排错** | `manuals/` 里对应的手册，**只此一处** | 混音 4 件事 · 扒带 6 步 · Praat 操作 · 故障库字段 · ACE-Step 的 8GB 应对 · 知识地图 |
| **排期 / 任务 / 验收 / 完成状态**（计数型积累目标在 `accumulators` → §8.2） | `progress/plan90.json`；`03` 逐周表自动生成，`04` 仅作分步模板 | "W2 扒根音" · "和声笔记及对账结果" · "好听元素清单 30 条" |
| **学习方法 / 技术数据的极简镜像** | `00-速查表.md`（**唯一被允许的例外**） | 每个 Step 一行、不写解释、必带 `→` 指向手册 |

**执行层可写短动作与验收，不再展开第二套操作教程 / 知识表。** 发现重复严格按序：先确认手册有完整版（没有就**先迁进去；绝不在内容还没有新家之前删掉它**）→ 执行层只留一行 `→ 手册 §X` → 带验收标准的内容（如"好听元素清单 30 条"）**标准留执行层、方法去手册**。

**本人明确要求 / 01 已确认约束优先；方法看 `manuals/`，实际日期与状态看 JSON。** 改方法不覆盖完成记录。README 保留导航与目标窗口摘要、不滚动抄状态；手册 / 04 对接表只概括初始模板，重排后以 JSON 为准。`.agents/skills/` 是上游通用规范、不代表当前安装状态；常规文档同步不凭空创建 `docs/`、不改上游 skill 正文。

---

## 7. 工具与记录维护规范

日常 CLI：**Python 3.11+ 标准库**；静态 HTML 直接打开，无服务 / 数据库 / 环境变量。Node 20 仅维护测试用，Playwright 仅可选浏览器测试用（安装见 AI 手册 §4.6）。

| 规则 | 说明 |
|---|---|
| **零外部依赖** | `tools/*.html` 单文件、内联 JS / CSS / JSON，无 CDN / 外部字体 / 外部脚本 |
| **唯一来源** | 进度 `progress/plan90.json`；真实故障 `data/faults.local.json`；不是 localStorage，不向用户索取截图查进度 |
| **浏览器边界** | 默认打开文件快照；缓存只能显式恢复为草稿，绝不自动覆盖；直写须授权 + 串行 + 检测外部改动，失败不报成功（细则 → AI 手册 §4.6） |
| **私有视图** | 故障原始内容仅进 `*.local.*`；公开 `tools/故障模式库.html` 必须保持空白工具壳 |
| **生成方向** | JSON → 总览 / HTML / `03` 逐周表。禁止从 HTML 反向抽取重建 JSON |
| **导入边界** | 异常记录整包停止、正本不变；不猜日期、不静默过滤；未知 ID 归档、不计完成 |
| **保留状态** | 增删改排期保留 ID、done、completedOn、note、evidence、周备注、checkins、dailyLogs、history、`_orphans` 及未知字段 |
| **写入安全** | 优先 `scripts/progress.py`（原子写 / 冲突检测 / `.backups/` 5 版）；非跨进程事务，手改 JSON 后校验同步 |
| **校验** | 数据 / 视图一致性、HTML 标签与 JS 语法、文档及隐私检查均要通过 |

---

## 8. 每日接管协议（用户只需要对话）

1. **读文件，不凭聊天记忆**。日期按 **Asia/Shanghai**；用户明确要查别的日期用 `--date`；不把旧 README 的"今天"当今天。
2. **问任务只读**：1 件优先 + ≤2 件顺带；夜班只留 10 分钟、不堆欠账；音乐落后含未通过的发布验收；IPA 不挤占音乐。
3. **完成需本人确认**（WF2）：含糊的"做完了"先核对是哪项；小步骤只记备注、不勾整项；日期到了 / 工具建好 / 目录存在都不等于做完。
4. **打卡独立于任务**：确认实际做过且要求记录才记；不虚构连续天数；发布需真实链接 + 四项验收。
5. **写完即核验并同步**：回复实际勾了哪些、还差什么；不擅自发帖 / push、不替用户定缩减三首目标。
6. **故障如实录入**：只整理本人提供的信息、未知留空；示例不计成果；达到数量不自动勾周目标。
7. **重排有留痕**：ID 不变、历史不删；夜班模式只暂停推荐，顺延截止日需另行确认重排。

以下从项目根目录执行（Windows 的 `python3` 按已安装解释器换 `python` / `py -3`）：

```bash
python3 scripts/progress.py today            # 只读；查别的日期加 --date
python3 scripts/progress.py fault list
python3 scripts/sync-plan.py --check
python3 scripts/check-docs.py
python3 -m unittest discover -s scripts -p 'test_*.py'   # 跨语言对照：node scripts/test-file-store.cjs
```

写入 / 撤销 / 豁免 / 导入 / 脱敏全命令见 AI 手册 §4.6 与 `progress.py --help`（含 check-tools / browser-smoke）；**没有本人确认，不照抄示例勾选或打卡**。CI 在 push / PR / 手动触发跑只读检查与隔离测试，不部署、不污染真实记录；审计先登记代码问题，经授权修复后补负例再验收。

### 8.1 预设工作流：每日闭环（问 → 报 → 勾 → 预告）

> 目标：用户一开口，**默认只跑 1 条命令**就走完闭环；token 纪律：`today` 单命令 ＞ 定向抽取 ＞ 全文读取，不通读 `02` / 手册 / 本文件；提示与知识一律指向既有文件章节，不现场发明。

**WF1 · 问任务**（"今天要做什么 / 这周干什么 / 进度怎样"）

| 步 | 动作 |
|---|---|
| ① 时间 | 用环境注入的 Asia/Shanghai 当天日期；没有注入时才跑 `date`，不为此多读文件 |
| ② 状态 | 只跑 `python3 scripts/progress.py today`（别的日期加 `--date`）→ 一条输出覆盖：日期 / 状态 / 当天优先动作 / **当周剩余** / **积累（+≤1 提醒）** / **下一里程碑** / **明日预告** / 打卡状态 |
| ③ 回复 | 当天 1 件优先（+≤2 顺带）→ 当周重点与剩余 → 进度（任务 / 交付 / 打卡）→ **积累（一行状态 + ≤1 提醒，见 §8.2）** → 注意点（逾期 / 夜班 / 里程碑）→ 1 条贴士（下表）→ 提醒打卡，并请用户报当天实际做完的事项 |
| ⛔ | 不通读 `03` / `进度总览` / 手册；用户追问某周细节才用 `sed -n '/^## W<n> /,/^## W<n+1> /p' progress/进度总览.md` 定向抽取 |

**贴士表**（按状态触发；每次最多 1 条；只引用既有文件，不新增任务）：

| 触发状态 | 贴士 |
|---|---|
| 音乐线落后 / `ipaPaused` | IPA 停掉，先对着 `04` 作业单走；卡点查 `音乐手册` §2.11 |
| 扒带卡住 | `00` §3 六步：分轨提 bass → 降速 50% → 先扒再对账，不先查答案 |
| 发布临近（到期前 2 天内） | 对照完成定义四件套（`00` §10），不加新制作环节 |
| 周六设备块 | 先给**设备学习处方**（知识点 → `半导体手册` §2.2 / §2.7 四步法 ≤60 分钟），再挂靠一条已记故障，然后录故障（`00` §9） |
| 故障根因落在某知识块 | 顺带指一句：正好配 `半导体手册` §2.2 对应块一起学（点到为止，不强加） |
| 工作日墨墨做完 | 微剂量可选：1 个术语词或 1 句故障复盘 ≤2′，可跳过（`半导体手册` §2.7） |
| 日语线 | Kaishi 新卡 ≤10 张 / 天；歌词听读并入耳朵 15′（`00` §7） |
| 周日晚 | 周回顾三行 + 定下周唯一大事（`03`） |

**WF2 · 报进展与打卡**（"我做了 X / 帮我打卡"）

1. 对到稳定 ID；不确定就先问清是哪项，不猜。一条报告可对应多个 ID。
2. 一小步 → `note`；**整项且用户明确确认** → `done`（发布 / 交付项须 `--evidence` 真实链接）。
3. 用户确认实际学过并要求记录 → `checkin`（`--on` 实际日期；同日不重复累计；整理工具不算学习）。
4. **积累上报**（"好听元素清单报一行 / 术语表到 N 词"）→ 按 §8.2 用 `accumulate`；只认本人确认的数字，不猜不凑。
5. 写完重跑一次 `today`，回复：勾了哪些 ID、还差什么、**明日预告 + 后续里程碑**，并提示当周 / 当月要记得的节点。

**WF3 · 夜班 / 低能**：确认日期范围 → `mode --kind night|low`；说明豁免 ≠ 完成，不打卡、不堆欠账。
**WF4 · 故障口述**：按 `半导体设备成长手册` §2.3 字段整理 → `fault add`（输入用 `.local.json`）；未知根因 / 时长留空，不编造。

### 8.2 积累项：计数型目标（每日闭环必须带出来）

> 点状任务有截止日；**计数型目标没有截止日——不在每日闭环里出现，就会静默归零**（01 §7.2 钉子 1 / 4）。
> 状态在 `plan90.json` 的 `accumulators`（唯一来源，见 §6.1）；方法走对应手册。本节只定「怎么报、怎么提醒」。

| 项 | 模式 | 用户怎么说 → agent 怎么做 |
|---|---|---|
| **好听元素清单** 30 条（W1 起积累，W9 原创 `9-0` 消费） | `items`：原文入 JSON | 口述一行 `[歌名] [时间戳] [具体描述]` → `accumulate taste --text "…"`（`音乐手册` §2.9） |
| **英语术语表** 80 词（Day 90 交付） | `reported`：原文在外部（墨墨 / 手册阅读），只记累计数 | "术语表到 N 词" → `accumulate terms --set N`（只增；更正需 `--force`；`半导体手册` §2.6） |
| **故障模式库** 30 条（Day 90 交付） | `derived`：实时派生自 `faults.local.json` | 只走 `fault add`，**不手设计数**（`半导体手册` §2.3） |

- 提醒由 `progress.py` 派生（计数 / 下一档 / 距上次上报天数），**至多 1 条**：下一档 ≤3 天内、或 ≥2 天无新条目才触发；夜班模式不显示；Day 0 不显示。
- 累计数只认本人确认，不猜不凑；不把计数写进任务 `note`；不为此新建第四份账或新时间块。

---

## 9. 禁区清单

- 新增第五条轨道
- 建议买设备 / 买乐器 / 买插件（已约定 90 天内不买）
- 把联网 / 云服务作为新执行前置：默认工作流用已有本地工具，外部网页只作可选查阅；HTML 必须离线自足。实际公开发布仍按已确认目标执行，不擅自代发
- 给"报班 / 考证 / 找老师"类建议（2027 年之前不适用）
- 把 `manuals/` 里的内容搬进执行层（会让周计划变重）
- ⛔ **提交未脱敏的内容**：真实机台编号 / 报警代码 / 薪资 / 厂区与产线规格 / 他人姓名（敏感范围边界见 `01` 头部声明，2026-09-07 本人确认）。需要记的用 `*.local.*` 后缀；进度 JSON 与普通 HTML / Markdown 中不能写真值。
- ⛔ **本地文件不等于不上云**：Arena 工作区不是用户 Windows 硬盘。企业原始日志不得带到未经授权的云端 agent；只用获准的本机环境或脱敏摘要。
- 建议用户学习编程（**已确认：他对编程能力无任何要求和预期**）

---

## 10. 一句话

> **这份体系的敌人是"更多"，不是"不够"。**
> 任何增加复杂度、增加选项、增加文件、增加轨道的改动，默认都是错的。

---

## 11. 🔒 本地真值映射（脱敏仓库的还原机制）

**公开文件使用代称；整个工作目录含私有文件，不能直接当公开发布包。** `01` / `02` / `README` / `半导体手册` 里的"华东 A 厂""华东某市"等都是**占位符**。

**若工作区存在 `01-个人档案.local.md`（gitignored，CI 里没有），先读它**：一张「占位符 → 真值」映射表。读档案时把真值替换回去再理解——这样本地看到的信息完整，不因脱敏而判断失真。

三条硬规矩：

1. ⛔ **不要把真值写进任何产出物** —— commit message、文档、代码、issue、对话里对外的部分
2. ⛔ **不要"顺手"把真值填回公开文件** —— 那会直接污染仓库
3. ✅ 引用时继续用占位符；分析不依赖真值

> **改完任何文档后跑 `python3 scripts/check-docs.py`** —— 它反用该映射表把每个真值在所有公开文件里搜一遍，命中即报 `隐私` 失败（映射表同时是"真值清单"与"泄露检测词表"）。
